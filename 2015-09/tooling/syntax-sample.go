package main

import "fmt"

var line int

func writeLine(s string) {
	fmt.Printf("%d: %s\n", line, s)
}

func main() {
	writeLine("First printing")
	writeLine("Second")
	writeLine("It keeps going")
}

func bob() {
	position := initial + rate*60
	if a == b {
		return
	}
}
